using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Sickle : Tool
{
    internal static Sickle sickle;

    // Start is called before the first frame update
    void Awake()
    {
        sickle = this;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
